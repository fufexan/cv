#import "../src/template.typ": *

#cvSection("Skills")

#cvSkill(
	type: [Spoken languages],
	info: [English #hBar() Romanian],
)

#cvSkill(
	type: [Prog. languages],
	info: [Bash #hBar() C/C++ #hBar() Java #hBar() JavaScript #hBar() Nix #hBar()
	PHP #hBar() Python #hBar() Rust],
)

#cvSkill(
	type: [Databases],
	info: [MySQL #hBar() Firestore],
)

#cvSkill(
	type: [Build tools],
	info: [Meson #hBar() Nix],
)

#cvSkill(
	type: [DevOps],
	info: [NixOS #hBar() QEMU #hBar() Terraform],
)

#cvSkill(
	type: [Social skills],
	info: [Worked in multiple pairs and teams, participated in team-building
	activities. Working with people is a strong point.],
)
